# ToDo管理アプリ 画面設計書

## 1. 文書の目的

本書は、`handout_app_spec.md`、`requirements.md`、`db-design.md`をもとに、ToDo管理アプリの画面に表示する項目、言葉、操作、画面遷移（画面から別の画面への移り方）を定める。

対象画面は次の6画面だけとし、画面を追加しない。既存のトップページ `/` は本書の設計対象に含めない。

| 画面ID | 画面名 | アドレス | 役割 |
|---|---|---|---|
| SC-01 | 一覧 | `/todos` | やることの一覧表示、絞り込み、並び替え、各操作への入口 |
| SC-02 | 登録 | `/todos/new` | 新しいやることの入力 |
| SC-03 | 登録の確認 | `/todos/confirm` | 登録内容の確認と登録の実行 |
| SC-04 | 編集 | `/todos/{id}/edit` | 登録済みのやることの編集 |
| SC-05 | 編集の確認 | `/todos/{id}/confirm` | 編集内容の確認と保存の実行 |
| SC-06 | 削除確認 | `/todos/{id}/delete` | 削除対象の確認と削除の実行 |

`{id}` は、1件のやることを見分ける番号に置き換える部分である。

## 2. 全画面共通の設計

### 2.1 見た目と配置

- 6画面すべてで、共通のCSS（画面の色や大きさを決める仕組み）である `/css/style.css` だけを読み込む。
- CSSの内容は、`handout_app_spec.md` の「6-3. CSS」に記載された内容を変更せず使用する。
- 画面全体を `container`、画面の見出しを `page-title` で表示する。
- 影、動き、画像、外部CSSは使用しない。
- 画面内に見た目を直接指定せず、使用するclass（見た目を割り当てる名前）は次だけとする。

| class名 | 使用目的 |
|---|---|
| `container` | 画面全体の入れ物 |
| `page-title` | 画面の見出し |
| `todo-table` | 一覧の表 |
| `flash` | 直前の操作結果 |
| `search-bar` | 絞り込みと並び替えのまとまり |
| `form-row` | 入力欄1行のまとまり |
| `form-label` | 入力欄の名前 |
| `form-input` | 入力欄・選択欄 |
| `error` | 入力エラーの文 |
| `confirm-value` | 確認画面に表示する値 |
| `confirm-actions` | 確認画面のボタンの並び |
| `btn` | 主なボタン |
| `btn sub` | 「書き直す」「やめる」「一覧へ」の副ボタン |
| `link` | 「編集」「削除」と並び替えの文字リンク |

### 2.2 データの表示方法

| 対象 | 保存される値 | 画面に表示する言葉 |
|---|---|---|
| 優先度 | `1` / `2` / `3` | 「高」/「中」/「低」 |
| 完了状態 | `1` / `0` | 「完了」/「未完了」 |
| 確認画面の未入力のメモ・期限 | 値なし | 「（入力なし）」 |

`id`、`created_at`（登録日時）、`updated_at`（最終更新日時）は画面の入力欄に表示しない。

### 2.3 入力エラーの共通ルール

登録画面と編集画面で「確認する」を押したときに入力を確認する。誤りがある場合は確認画面へ移動せず、入力値を残したまま同じ入力画面を表示し、該当する欄の直下に次の文を `error` で表示する。

| 対象欄 | 誤りの条件 | 表示するエラー文 |
|---|---|---|
| やること | 空、半角空白だけ、または全角空白だけ | 「やることを入力してください」 |
| やること | 255文字を超える | 「やることは255文字以内で入力してください」 |
| メモ | 255文字を超える | 「メモは255文字以内で入力してください」 |
| ジャンル | 未選択 | 「ジャンルを選んでください」 |
| 優先度 | 未選択 | 「優先度を選んでください」 |

期限には追加の入力ルールを設けない。

## 3. 画面ごとの設計

### 3.1 SC-01 一覧

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos` |
| 画面の見出し | 「やること一覧」 |
| 使用するclass | `container`、`page-title`、`flash`、`search-bar`、`form-input`、`btn`、`todo-table`、`link` |

#### 上からの表示順

1. 直前の操作結果。表示する結果がある場合だけ1行表示する
2. 画面の見出し「やること一覧」
3. 絞り込みと並び替えのまとまり
4. 「新規登録」ボタン
5. やることの一覧表。0件の場合は表の代わりに0件時の文を表示する

#### 直前の操作結果

| 状況 | 表示する文 |
|---|---|
| 登録が完了した | 「登録しました」 |
| 編集内容の保存が完了した | 「保存しました」 |
| 削除が完了した | 「削除しました」 |
| 存在しない番号で編集、編集の確認、または削除確認を開いた | 「見つかりませんでした」 |

#### 絞り込みと並び替え

| 種別 | 画面に表示する言葉 | 部品と選択肢 | 操作結果 |
|---|---|---|---|
| 名前の絞り込み | 「やること」 | 1行の入力欄 | 入力した言葉を一部に含む「やること」だけを表示する |
| ジャンルの絞り込み | 「ジャンル」 | 選択欄。「すべて」「デザイン」「マーケティング」「プログラミング」「資格」「就職活動」 | 「すべて」以外では、選んだジャンルと同じものだけを表示する |
| 実行ボタン | 「絞り込む」 | ボタン | 指定した条件で同じ一覧画面を開き直す |
| 昇順の並び替え | 「期限が近い順」 | 文字リンク | 期限が近い順で同じ一覧画面を開き直す |
| 降順の並び替え | 「期限が遠い順」 | 文字リンク | 期限が遠い順で同じ一覧画面を開き直す |

並び順の1段目は印の有無とし、印ありを常に上に表示する。2段目は期限順とし、最初に一覧を開いたときは「期限が近い順」とする。期限が未入力のデータは、同じ印の有無の中で「期限が近い順」と「期限が遠い順」のどちらでも末尾に表示する。名前とジャンルの両方が空の場合は全件を表示する。絞り込み条件と並び順はクエリパラメータ（アドレスの `?` より後ろに付ける条件）で保持する。例は `/todos?keyword=課題&category=プログラミング&order=asc` とする。

#### 一覧表

| 左からの順番 | 列の見出し | 各行に表示する内容 |
|---:|---|---|
| 1 | 「印」 | 印なしの「☆」または印ありの「★」の切り替えボタン |
| 2 | 「やること」 | やることの文字 |
| 3 | 「ジャンル」 | 選ばれているジャンル |
| 4 | 「優先度」 | 「高」「中」「低」のいずれか |
| 5 | 「期限」 | 保存されている日付。未入力の場合は空欄 |
| 6 | 「状態」 | 「完了」または「未完了」 |
| 7 | 「操作」 | 文字リンク「編集」「削除」 |

登録データが0件の場合と絞り込み結果が0件の場合は、どちらも「まだ1件も登録されていません」と表示する。

#### 押せるものと移動先

| 押せるもの | 移動先・結果 |
|---|---|
| 「絞り込む」 | 条件を付けて一覧 `/todos` を再表示する |
| 「期限が近い順」 | `order=asc` を付けて一覧 `/todos` を再表示する |
| 「期限が遠い順」 | `order=desc` を付けて一覧 `/todos` を再表示する |
| 各行の「☆」または「★」 | 印の有無を切り替え、絞り込み条件、期限順、ページを保って一覧を再表示する |
| 「新規登録」 | 登録 `/todos/new` へ移動する |
| 各行の「編集」 | 対象の編集 `/todos/{id}/edit` へ移動する |
| 各行の「削除」 | 対象の削除確認 `/todos/{id}/delete` へ移動する |

### 3.2 SC-02 登録

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos/new` |
| 画面の見出し | 「やることを登録」 |
| 使用するclass | `container`、`page-title`、`form-row`、`form-label`、`form-input`、`error`、`btn`、`btn sub` |

#### 上からの表示順と入力欄

| 順番 | 欄に表示する名前 | 必須 | 欄の形 | 初期表示・選択肢 |
|---:|---|---|---|---|
| 1 | 「やること」 | 必須 | 1行の文字入力 | 空欄 |
| 2 | 「メモ」 | 任意 | 1行の文字入力 | 空欄 |
| 3 | 「ジャンル」 | 必須 | 選択欄 | 最初は未選択。「デザイン」「マーケティング」「プログラミング」「資格」「就職活動」 |
| 4 | 「優先度」 | 必須 | 選択欄 | 最初は未選択。「高」「中」「低」 |
| 5 | 「期限」 | 任意 | 日付入力 | 空欄 |

各入力欄は `form-row` の中に、`form-label` の名前、`form-input` の欄、必要な場合は `error` のエラー文の順で置く。

#### ボタンと移動先

| ボタン名 | 移動先・結果 |
|---|---|
| 「確認する」 | 入力が正しければ、入力値を保持して登録の確認 `/todos/confirm` へ移動する。入力エラーがあれば登録画面に留まる |
| 「一覧へ」 | 入力内容を保存せず、一覧 `/todos` へ移動する |

### 3.3 SC-03 登録の確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos/confirm` |
| 画面の見出し | 「登録内容の確認」 |
| 使用するclass | `container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub` |

#### 上からの表示順

入力欄は置かず、次の名前と値を文字として表示する。未入力の「メモ」と「期限」には「（入力なし）」と表示する。

| 順番 | 表示する名前 | 表示する値 |
|---:|---|---|
| 1 | 「やること」 | 登録画面で入力した文字 |
| 2 | 「メモ」 | 入力した文字、または「（入力なし）」 |
| 3 | 「ジャンル」 | 選んだジャンル |
| 4 | 「優先度」 | 「高」「中」「低」のいずれか |
| 5 | 「期限」 | 入力した日付、または「（入力なし）」 |

#### ボタンと移動先

| ボタン名 | 移動先・結果 |
|---|---|
| 「登録する」 | 1件を保存し、「登録しました」を表示する一覧 `/todos` へ移動する |
| 「書き直す」 | 保存せず、入力値を保持した登録 `/todos/new` へ戻る |

### 3.4 SC-04 編集

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos/{id}/edit` |
| 画面の見出し | 「やることを編集」 |
| 使用するclass | `container`、`page-title`、`form-row`、`form-label`、`form-input`、`error`、`btn`、`btn sub` |

#### 上からの表示順と入力欄

画面を開いた時点では、対象の現在の値を各欄に表示する。

| 順番 | 欄に表示する名前 | 必須 | 欄の形 | 表示内容・選択肢 |
|---:|---|---|---|---|
| 1 | 「やること」 | 必須 | 1行の文字入力 | 現在の文字 |
| 2 | 「メモ」 | 任意 | 1行の文字入力 | 現在の文字。値がなければ空欄 |
| 3 | 「ジャンル」 | 必須 | 選択欄 | 現在の値を選択。「デザイン」「マーケティング」「プログラミング」「資格」「就職活動」 |
| 4 | 「優先度」 | 必須 | 選択欄 | 現在の値を選択。「高」「中」「低」 |
| 5 | 「期限」 | 任意 | 日付入力 | 現在の日付。値がなければ空欄 |
| 6 | 「完了」 | 必須 | チェック欄 | 現在が完了ならチェックあり、未完了ならチェックなし |

#### ボタンと移動先

| ボタン名 | 移動先・結果 |
|---|---|
| 「確認する」 | 入力が正しければ、編集値を保持して編集の確認 `/todos/{id}/confirm` へ移動する。入力エラーがあれば編集画面に留まる |
| 「一覧へ」 | 変更を保存せず、一覧 `/todos` へ移動する |

対象の `id` が存在しない場合は専用画面を出さず、「見つかりませんでした」を表示する一覧 `/todos` へ移動する。

### 3.5 SC-05 編集の確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos/{id}/confirm` |
| 画面の見出し | 「編集内容の確認」 |
| 使用するclass | `container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub` |

#### 上からの表示順

入力欄は置かず、次の名前と値を文字として表示する。未入力の「メモ」と「期限」には「（入力なし）」と表示する。

| 順番 | 表示する名前 | 表示する値 |
|---:|---|---|
| 1 | 「やること」 | 編集画面で入力した文字 |
| 2 | 「メモ」 | 入力した文字、または「（入力なし）」 |
| 3 | 「ジャンル」 | 選んだジャンル |
| 4 | 「優先度」 | 「高」「中」「低」のいずれか |
| 5 | 「期限」 | 入力した日付、または「（入力なし）」 |
| 6 | 「完了」 | 「完了」または「未完了」 |

#### ボタンと移動先

| ボタン名 | 移動先・結果 |
|---|---|
| 「保存する」 | 変更を保存し、「保存しました」を表示する一覧 `/todos` へ移動する |
| 「書き直す」 | 保存せず、編集値を保持した編集 `/todos/{id}/edit` へ戻る |

対象の `id` が存在しない場合は専用画面を出さず、「見つかりませんでした」を表示する一覧 `/todos` へ移動する。

### 3.6 SC-06 削除確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| アドレス | `/todos/{id}/delete` |
| 画面の見出し | 「削除の確認」 |
| 使用するclass | `container`、`page-title`、`confirm-value`、`confirm-actions`、`btn`、`btn sub` |

#### 上からの表示順

入力欄は置かず、削除する1件の内容を文字として表示する。

| 順番 | 表示する名前 | 表示する値 |
|---:|---|---|
| 1 | 「やること」 | 保存されている文字 |
| 2 | 「ジャンル」 | 保存されているジャンル |
| 3 | 「期限」 | 保存されている日付。値がなければ「（入力なし）」 |

#### ボタンと移動先

| ボタン名 | 移動先・結果 |
|---|---|
| 「消す」 | 対象を削除し、「削除しました」を表示する一覧 `/todos` へ移動する |
| 「やめる」 | 削除せず、一覧 `/todos` へ移動する |

対象の `id` が存在しない場合は専用画面を出さず、「見つかりませんでした」を表示する一覧 `/todos` へ移動する。

## 4. 画面遷移一覧

画面遷移（画面から別の画面への移り方）の全体は次のとおりとする。

| 現在の画面 | 操作 | 次の画面 | 補足 |
|---|---|---|---|
| 一覧 | 「新規登録」 | 登録 | 新しい入力を始める |
| 登録 | 「確認する」 | 登録の確認 | 入力が正しい場合だけ移動する |
| 登録 | 「一覧へ」 | 一覧 | 入力内容は保存しない |
| 登録の確認 | 「登録する」 | 一覧 | 登録後に「登録しました」を表示する |
| 登録の確認 | 「書き直す」 | 登録 | 入力値を保持する |
| 一覧 | 行の「編集」 | 編集 | 選んだ行の現在値を表示する |
| 編集 | 「確認する」 | 編集の確認 | 入力が正しい場合だけ移動する |
| 編集 | 「一覧へ」 | 一覧 | 変更内容は保存しない |
| 編集の確認 | 「保存する」 | 一覧 | 保存後に「保存しました」を表示する |
| 編集の確認 | 「書き直す」 | 編集 | 編集値を保持する |
| 一覧 | 行の「削除」 | 削除確認 | 選んだ行の削除対象を表示する |
| 削除確認 | 「消す」 | 一覧 | 削除後に「削除しました」を表示する |
| 削除確認 | 「やめる」 | 一覧 | 削除しない |
| 一覧 | 「絞り込む」 | 一覧 | 絞り込み条件をアドレスに保持する |
| 一覧 | 「期限が近い順」 | 一覧 | 絞り込み条件を保ったまま近い順にする |
| 一覧 | 「期限が遠い順」 | 一覧 | 絞り込み条件を保ったまま遠い順にする |
| 編集・編集の確認・削除確認 | 存在しない `id` を指定 | 一覧 | 「見つかりませんでした」を表示する |

## 5. 画面に表示する言葉の一覧

### 5.1 見出し

- 「やること一覧」
- 「やることを登録」
- 「登録内容の確認」
- 「やることを編集」
- 「編集内容の確認」
- 「削除の確認」

### 5.2 項目名・表の列名

- 「やること」
- 「メモ」
- 「ジャンル」
- 「優先度」
- 「期限」
- 「完了」
- 「状態」
- 「操作」

### 5.3 ボタン・リンク

- 「絞り込む」
- 「期限が近い順」
- 「期限が遠い順」
- 「新規登録」
- 「編集」
- 「削除」
- 「確認する」
- 「一覧へ」
- 「登録する」
- 「書き直す」
- 「保存する」
- 「消す」
- 「やめる」

### 5.4 選択肢・状態表示

- ジャンル：「すべて」「デザイン」「マーケティング」「プログラミング」「資格」「就職活動」
- 優先度：「高」「中」「低」
- 状態：「完了」「未完了」
- 任意項目の未入力表示：「（入力なし）」

### 5.5 通知・0件表示・エラー

- 「登録しました」
- 「保存しました」
- 「削除しました」
- 「見つかりませんでした」
- 「まだ1件も登録されていません」
- 「やることを入力してください」
- 「やることは255文字以内で入力してください」
- 「メモは255文字以内で入力してください」
- 「ジャンルを選んでください」
- 「優先度を選んでください」

## 6. 要件との対応

| 画面 | 主な対応要件 |
|---|---|
| SC-01 一覧 | FR-01、FR-05、FR-06、ER-01、ER-02、NR-01～NR-04、SR-01 |
| SC-02 登録 | FR-02-01～FR-02-03、FR-02-07、VR-01～VR-05、SR-02 |
| SC-03 登録の確認 | FR-02-04～FR-02-06、SR-03 |
| SC-04 編集 | FR-03-01～FR-03-03、FR-03-07、VR-01～VR-05、ER-01、ER-02、SR-04 |
| SC-05 編集の確認 | FR-03-04～FR-03-06、ER-01、ER-02、SR-05 |
| SC-06 削除確認 | FR-04-01～FR-04-04、ER-01、ER-02、SR-06 |
| 6画面共通 | DR-01～DR-09、NFR-04～NFR-09 |

## 7. テンプレート画面

全画面で `container`、`page-title`、`btn`、`btn sub`、`link` を既存用途どおり使い、一覧・項目表示には `template-list`、`template-card`、`template-item`、`item-meta`、操作群には `template-actions` を使う。入力には `form-row`、`form-label`、`form-input`、`error`、確認には `confirm-value`、`confirm-actions` を使う。

| 画面 | URL・メソッド | 表示・入力 | 操作・遷移 |
|---|---|---|---|
| SC-08 テンプレート一覧 | `GET /todo-templates` | 名前、項目数、全項目。0件時「テンプレートはまだ登録されていません」。通知を表示 | 新規登録、編集、削除、適用、`/todos` |
| SC-09 テンプレート登録 | `GET /todo-templates/new` | 名前と1件以上の項目（タイトル、メモ、ジャンル、優先度、期限） | 項目追加・削除、確認、一覧へ |
| SC-10 登録確認 | `POST /todo-templates/confirm` | 入力欄でなく全文字表示。空のメモ・期限は「（入力なし）」 | `POST /todo-templates` で登録、`POST /todo-templates/new` で書き直し |
| SC-11 テンプレート編集 | `GET /todo-templates/{id}/edit` | 保存済みの名前と全項目を入力欄に表示 | 項目追加・削除、確認、一覧へ |
| SC-12 編集確認 | `POST /todo-templates/{id}/confirm` | SC-10と同形式 | `POST /todo-templates/{id}` で保存、`POST /todo-templates/{id}/edit` で書き直し |
| SC-13 削除確認 | `GET /todo-templates/{id}/delete` | 名前と全項目を読み取り専用表示 | `POST /todo-templates/{id}/delete` で削除、一覧へ戻る |
| SC-14 適用 | `GET /todo-templates/{id}/apply` | 作成対象の全項目を読み取り専用表示 | `POST /todo-templates/{id}/apply/confirm` で確認、一覧へ戻る |
| SC-15 適用確認 | `POST /todo-templates/{id}/apply/confirm` | 作成予定の全ToDoを読み取り専用表示 | `POST /todo-templates/{id}/apply` で一括作成、適用へ戻る |

登録・編集では、名前必須・255文字以内・空白だけ不可、項目1件以上、各タイトル必須・255文字以内・空白だけ不可、メモ255文字以内、ジャンルは5種類、優先度1～3、期限任意をサーバー側で検証する。JavaScriptは項目の追加・削除・連番化だけを補助し、最低1項目を画面上で維持する。確認画面のhidden値も最終POSTで再検証する。

編集・削除・適用関連の各段階でIDが存在しない場合は「見つかりませんでした」をフラッシュ表示して一覧へ戻す。成功通知は「テンプレートを登録しました」「テンプレートを保存しました」「テンプレートを削除しました」「N件のToDoを作成しました」。既存 `/todos` に「テンプレート一覧」、テンプレート一覧に「やること一覧へ」の相互導線を置く。
