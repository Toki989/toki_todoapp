<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todoapp.TodoMapper">
    <sql id="filterConditions">
        <if test="showCompleted == false">
            AND completed = false
        </if>
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
    </sql>

    <select id="search" resultType="com.example.todoapp.Todo">
        SELECT
            id,
            title,
            detail,
            category,
            priority,
            due_date,
            completed,
            completed_at,
            deleted_at,
            created_at,
            updated_at
        FROM todos
        WHERE deleted_at IS NULL
            <include refid="filterConditions" />
            <if test="from != null">
                AND due_date &gt;= #{from}
            </if>
            <if test="to != null">
                AND due_date &lt;= #{to}
            </if>
        ORDER BY due_date IS NULL ASC,
        <choose>
            <when test="order == 'desc'">
                due_date DESC
            </when>
            <otherwise>
                due_date ASC
            </otherwise>
        </choose>
    </select>

    <select id="searchPage" resultType="com.example.todoapp.Todo">
        SELECT
            id,
            title,
            detail,
            category,
            priority,
            due_date,
            completed,
            completed_at,
            deleted_at,
            created_at,
            updated_at
        FROM todos
        WHERE
        <choose>
            <when test="trash">
                deleted_at IS NOT NULL
            </when>
            <otherwise>
                deleted_at IS NULL
            </otherwise>
        </choose>
        <include refid="filterConditions" />
        ORDER BY due_date IS NULL ASC,
        <choose>
            <when test="order == 'desc'">
                due_date DESC
            </when>
            <otherwise>
                due_date ASC
            </otherwise>
        </choose>,
        id ASC
        LIMIT 10 OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM todos
        WHERE
        <choose>
            <when test="trash">
                deleted_at IS NOT NULL
            </when>
            <otherwise>
                deleted_at IS NULL
            </otherwise>
        </choose>
        <include refid="filterConditions" />
    </select>

    <select id="findById" resultType="com.example.todoapp.Todo">
        SELECT
            id,
            title,
            detail,
            category,
            priority,
            due_date,
            completed,
            completed_at,
            deleted_at,
            created_at,
            updated_at
        FROM todos
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="com.example.todoapp.Todo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (
            title,
            detail,
            category,
            priority,
            due_date,
            completed
        ) VALUES (
            #{title},
            #{detail},
            #{category},
            #{priority},
            #{dueDate},
            COALESCE(#{completed}, false)
        )
    </insert>

    <update id="update" parameterType="com.example.todoapp.Todo">
        UPDATE todos
        SET
            title = #{title},
            detail = #{detail},
            category = #{category},
            priority = #{priority},
            due_date = #{dueDate},
            completed_at = CASE
                WHEN completed = false AND COALESCE(#{completed}, false) = true
                    THEN CURRENT_TIMESTAMP
                WHEN completed = true AND COALESCE(#{completed}, false) = false
                    THEN NULL
                ELSE completed_at
            END,
            completed = COALESCE(#{completed}, false)
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <update id="markDeleted">
        UPDATE todos
        SET deleted_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted_at IS NULL
    </update>

    <update id="restore">
        UPDATE todos
        SET deleted_at = NULL
        WHERE id = #{id}
          AND deleted_at IS NOT NULL
    </update>
</mapper>
