# ToDo管理アプリ 要件定義書

## 1. 文書の目的

本書は、`handout_app_spec.md` をもとに、ToDo管理アプリが満たす要件を整理したものである。

本書では「何ができなければならないか」を定める。DB設計（データをどのような表や列で保存するかの詳細設計）と画面設計（各画面の配置や部品を決める詳細設計）は別文書で作成する。

## 2. 対象システムの概要

本システムは、利用者が自分の「やること」を登録し、一覧表示、編集、削除、検索、並び替えによって管理するWebアプリケーション（ブラウザで利用するアプリ）である。

- 利用者は1人とする。
- ログイン機能は設けない。
- アプリの中心となる一覧画面のアドレスは `/todos` とする。
- トップページ `/` は既存のものをそのまま利用する。

## 3. 対象範囲

### 3.1 対象とする機能

| 要件ID | 機能 | 要件 |
|---|---|---|
| FR-01 | 一覧表示 | 登録されたやることを1画面に一覧表示できること。 |
| FR-02 | 登録 | 新しいやることを1件登録できること。 |
| FR-03 | 編集 | 登録済みのやることを編集できること。 |
| FR-04 | 削除 | 登録済みのやることを1件削除できること。 |
| FR-05 | 検索 | やることの名前とジャンルで一覧を絞り込めること。 |
| FR-06 | 並び替え | 期限が近い順または遠い順に一覧を並び替えられること。 |

### 3.2 対象外とする機能

次の機能は今回の開発対象に含めない。

- 一覧のページ分け（件数が多いときに複数ページへ分ける仕組み）
- 完了日時の記録
- ログイン機能
- 複数利用者による共有
- 写真やファイルの添付
- `handout_app_spec.md` に記載されていないその他の機能

## 4. 利用者要件

| 要件ID | 要件 |
|---|---|
| UR-01 | 利用者は、登録されているやることと、そのジャンル、優先度、期限、状態を一覧で確認できること。 |
| UR-02 | 利用者は、登録・編集・削除の前に対象内容を確認できること。 |
| UR-03 | 利用者は、入力内容に誤りがある場合、理由を確認して入力を直せること。 |
| UR-04 | 利用者は、直前に行った登録・編集・削除の結果を一覧画面で確認できること。 |
| UR-05 | 利用者は、名前またはジャンルで必要なやることを探せること。 |
| UR-06 | 利用者は、期限の近い順または遠い順にやることを確認できること。 |

## 5. 機能要件

### 5.1 一覧表示

| 要件ID | 要件 |
|---|---|
| FR-01-01 | 一覧には、左から「やること」「ジャンル」「優先度」「期限」「状態」「操作」を表示すること。 |
| FR-01-02 | 「操作」には、各データの「編集」と「削除」を表示すること。 |
| FR-01-03 | 優先度は、保存値 `1`、`2`、`3` を、それぞれ「高」「中」「低」と表示すること。 |
| FR-01-04 | 状態は、保存値 `1`、`0` を、それぞれ「完了」「未完了」と表示すること。 |
| FR-01-05 | 表示するデータが0件の場合、「まだ1件も登録されていません」と表示すること。検索結果が0件の場合も同じ文を使用すること。 |
| FR-01-06 | 一覧画面の上部に、名前の入力欄、ジャンルの選択欄、「絞り込む」ボタン、期限順の2つのリンク、「新規登録」ボタンを表示すること。 |
| FR-01-07 | 一覧画面の最上部に、直前の操作結果を1行で表示できること。 |

### 5.2 登録

| 要件ID | 要件 |
|---|---|
| FR-02-01 | 利用者は、「やること」「メモ」「ジャンル」「優先度」「期限」を入力できること。 |
| FR-02-02 | ジャンルと優先度は、最初は未選択の状態とすること。 |
| FR-02-03 | 「確認する」により、保存前に登録内容の確認へ進めること。 |
| FR-02-04 | 確認時は5項目を入力欄ではなく文字として表示し、未入力の任意項目は「（入力なし）」と表示すること。 |
| FR-02-05 | 「書き直す」により、入力値を保持したまま登録入力へ戻れること。 |
| FR-02-06 | 「登録する」により1件を保存し、一覧へ移動すること。 |
| FR-02-07 | 登録入力から「一覧へ」により、保存せず一覧へ戻れること。 |

### 5.3 編集

| 要件ID | 要件 |
|---|---|
| FR-03-01 | 編集では、登録時の5項目に「完了」のチェック欄を加えた6項目を扱うこと。 |
| FR-03-02 | 編集画面を開いたとき、現在保存されている値を各欄に表示すること。 |
| FR-03-03 | 「確認する」により、保存前に編集内容の確認へ進めること。 |
| FR-03-04 | 編集の確認は登録の確認と同じ表示形式とすること。 |
| FR-03-05 | 「書き直す」により、編集値を保持したまま編集入力へ戻れること。 |
| FR-03-06 | 「保存する」により変更を保存し、一覧へ移動すること。 |
| FR-03-07 | 編集入力から「一覧へ」により、変更を保存せず一覧へ戻れること。 |

### 5.4 削除

| 要件ID | 要件 |
|---|---|
| FR-04-01 | 削除前に、対象の「やること」「ジャンル」「期限」を文字として表示すること。期限が未入力の場合は「（入力なし）」と表示すること。 |
| FR-04-02 | 削除確認画面には入力欄を設けないこと。 |
| FR-04-03 | 「消す」により対象を削除し、一覧へ移動すること。 |
| FR-04-04 | 「やめる」により削除せず一覧へ戻れること。 |
| FR-04-05 | 削除では行を消さず、`deleted_at` に削除日時を記録すること。 |
| FR-04-06 | 通常の一覧、`/api/todos`、MCPの各操作では、`deleted_at` が入ったデータを対象外にすること。 |
| FR-04-07 | `/todos?trash=1` の場合だけ削除済みのデータを一覧表示すること。 |
| FR-04-08 | 削除済みの一覧から「戻す」を押すと、`deleted_at` を空に戻すこと。 |

### 5.5 検索と並び替え

| 要件ID | 要件 |
|---|---|
| FR-05-01 | 名前の絞り込みは、入力された文字列が `title` に一部でも含まれるデータを対象とすること。 |
| FR-05-02 | ジャンルの絞り込みは、選択された値と `category` が完全に一致するデータを対象とすること。 |
| FR-05-03 | ジャンルで「すべて」を選択した場合は、ジャンルによる絞り込みを行わないこと。 |
| FR-05-04 | 名前とジャンルの両方が空の場合は、すべてのデータを表示すること。 |
| FR-05-05 | 名前とジャンルを同時に指定できること。 |
| FR-06-01 | 並び替えは `due_date` を基準とすること。 |
| FR-06-02 | 一覧を最初に開いたときは、期限が近い順とすること。 |
| FR-06-03 | 「期限が近い順」と「期限が遠い順」を切り替えられること。 |
| FR-06-04 | 絞り込み条件と並び順は、クエリパラメータ（アドレスの `?` より後ろに付ける条件）として送ること。例は `/todos?keyword=課題&category=プログラミング&order=asc` とする。 |
| FR-06-05 | 期限が未入力のデータは、「期限が近い順」と「期限が遠い順」のどちらでも末尾に表示すること。 |

### 5.6 入力チェック

入力チェック（入力内容が決まりどおりかを確かめる処理）は、登録と編集の両方に適用する。

| 要件ID | 対象 | 保存を止める条件 | 表示する文 |
|---|---|---|---|
| VR-01 | やること | 空、または空白だけ | やることを入力してください |
| VR-02 | やること | 255文字を超える | やることは255文字以内で入力してください |
| VR-03 | メモ | 255文字を超える | メモは255文字以内で入力してください |
| VR-04 | ジャンル | 未選択 | ジャンルを選んでください |
| VR-05 | 優先度 | 未選択 | 優先度を選んでください |

- 必須項目は「やること」「ジャンル」「優先度」とする。
- 空白だけの入力には、半角空白と全角空白の両方を含む。
- 入力に誤りがある場合は保存せず入力画面に留まり、該当欄の下に指定の文を表示する。
- メモと期限は任意入力とする。
- 期限には入力内容に関する追加の決まりを設けない。

### 5.7 存在しないデータへの対応

| 要件ID | 要件 |
|---|---|
| ER-01 | 編集、編集の確認、または削除確認のアドレスに存在しない `id` が指定された場合、専用画面は表示せず一覧へ戻すこと。 |
| ER-02 | ER-01の場合、一覧の最上部に「見つかりませんでした」と表示すること。 |

### 5.8 操作結果の通知

| 要件ID | 操作 | 一覧に表示する文 |
|---|---|---|
| NR-01 | 登録完了 | 登録しました |
| NR-02 | 編集保存完了 | 保存しました |
| NR-03 | 削除完了 | 削除しました |
| NR-04 | 存在しないデータを指定 | 見つかりませんでした |

## 6. データ要件

データはMySQL（データを保存・検索するソフトウェア）の `todos` テーブル1つに保存し、1件のやることを1行として扱う。列の具体的な定義は、後で作成するDB設計書で本節の条件を満たすように決める。

| 要件ID | 項目 | 要件 |
|---|---|---|
| DR-01 | `id` | 各データを識別する番号。必須で、主キー（各行を重複なく見分ける値）とし、自動で1ずつ増やす。 |
| DR-02 | `title` | やること。必須。最大255文字。 |
| DR-03 | `detail` | メモ。任意。最大255文字。 |
| DR-04 | `category` | ジャンル。必須。指定された5種類のいずれか。 |
| DR-05 | `priority` | 優先度。必須。`1`＝高、`2`＝中、`3`＝低とし、既定値（入力指定がないときの値）は `2`。 |
| DR-06 | `due_date` | 期限。任意の日付。 |
| DR-07 | `completed` | 完了状態。必須。既定値は未完了。 |
| DR-08 | `created_at` | 登録日時。必須で、MySQLが自動設定する。 |
| DR-09 | `updated_at` | 最終更新日時。必須で、MySQLが自動設定する。 |
| DR-10 | `deleted_at` | 削除日時。任意。未削除と戻した後は空にする。 |

`id`、`created_at`、`updated_at` は利用者の入力項目にしない。

### 6.1 ジャンル

ジャンルは自由入力ではなく、次の5種類から選択する。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

## 7. 画面と移動の要件

本節では必要な画面と移動だけを定める。部品の細かな配置は、後で作成する画面設計書で決める。

| 要件ID | 画面 | アドレス | 主な役割 |
|---|---|---|---|
| SR-01 | 一覧 | `/todos` | 登録済みデータの表示、検索、並び替え、各操作への入口 |
| SR-02 | 登録 | `/todos/new` | 新しい1件の入力 |
| SR-03 | 登録の確認 | `/todos/confirm` | 登録内容の確認と登録実行 |
| SR-04 | 編集 | `/todos/{id}/edit` | 指定された1件の編集 |
| SR-05 | 編集の確認 | `/todos/{id}/confirm` | 編集内容の確認と保存実行 |
| SR-06 | 削除確認 | `/todos/{id}/delete` | 削除対象の確認と削除実行 |
| SR-07 | 消したもの | `/todos?trash=1` | 削除済みデータの表示と復元 |

`{id}` は対象データを識別する番号に置き換える。

画面移動は次のとおりとする。

- 一覧 → 登録 → 登録の確認 → 一覧
- 登録の確認 → 「書き直す」 → 入力値を保持した登録
- 一覧 → 編集 → 編集の確認 → 一覧
- 編集の確認 → 「書き直す」 → 編集値を保持した編集
- 一覧 → 削除確認 → 「消す」 → 一覧
- 削除確認 → 「やめる」 → 一覧
- 一覧 → 「消したものを見る」 → 消したもの → 「戻す」 → 消したもの
- 登録または編集 → 「一覧へ」 → 一覧

## 8. 非機能要件・制約

非機能要件（機能そのもの以外の、見た目や記録方法などの条件）と実装上の制約を次のとおりとする。

### 8.1 操作記録

| 要件ID | 要件 |
|---|---|
| NFR-01 | 登録、編集、削除が実行されたとき、操作ごとに1行のログ（動作の記録）を残すこと。 |
| NFR-02 | ログには、操作の種類（登録・編集・削除）と対象の `id` を記録すること。 |
| NFR-03 | ログには、`title` と `detail` の内容を記録しないこと。 |

### 8.2 見た目の共通制約

| 要件ID | 要件 |
|---|---|
| NFR-04 | 6画面すべてで、`src/main/resources/static/css/style.css` の1ファイルだけを共通利用すること。CSSは画面の見た目を指定する仕組みである。 |
| NFR-05 | 各画面の `<head>` で `<link rel="stylesheet" href="/css/style.css">` を読み込むこと。 |
| NFR-06 | 画面内に直接記述する `style="..."` と、2つ目のCSSファイルを使用しないこと。 |
| NFR-07 | 影、動き、画像、外部から読み込むCSSを使用しないこと。 |
| NFR-08 | 見た目は `handout_app_spec.md` の「6-3. CSS」に記載された内容を変更せず使用すること。 |
| NFR-09 | class（見た目を適用するために画面部品へ付ける名前）は次の一覧だけを使用し、新しい名前を追加しないこと。 |

使用可能なclassは次のとおりとする。

| class名 | 用途 |
|---|---|
| `container` | 画面全体の入れ物 |
| `page-title` | 画面タイトル |
| `todo-table` | 一覧の表 |
| `flash` | 直前の操作結果 |
| `search-bar` | 絞り込みと並び替えの帯 |
| `form-row` | 入力欄1行 |
| `form-label` | 入力欄の名前 |
| `form-input` | 入力欄・選択欄 |
| `error` | 入力エラーの文 |
| `confirm-value` | 確認画面の値 |
| `confirm-actions` | 確認画面のボタンの並び |
| `btn` | 主なボタン |
| `btn sub` | 書き直す・やめる・一覧へ等の副ボタン |
| `link` | 編集・削除・並び替えの文字リンク |

## 9. 受け入れ条件

受け入れ条件（完成と判断するための確認事項）は、次のとおりとする。

- FR-01からFR-06の6機能が利用できる。
- 登録と編集で、入力チェックと指定されたエラー文の表示が行われる。
- 登録、編集、削除の前に確認でき、書き直しまたは中止ができる。
- 存在しない `id` を指定した場合、一覧へ戻り、指定された通知文が表示される。
- 検索条件と並び順がアドレスに保持され、同じアドレスを開き直せる。
- 登録、編集、削除のログに操作種別と `id` が残り、`title` と `detail` は残らない。
- 対象外の機能が追加されていない。
- 見た目に関する共通制約が守られている。

## 10. 追加・補足事項

### 10.1 叩き台に対する機能追加

機能、保存項目、画面、入力ルールについて、`handout_app_spec.md` にない要件は追加していない。

### 10.2 整理のために追加した記載

次の記載は、叩き台の内容を追跡しやすくするための文書上の追加であり、アプリの機能追加ではない。

- 各要件の要件ID（`FR`、`UR`、`VR`、`ER`、`NR`、`DR`、`SR`、`NFR`）
- 「文書の目的」「対象範囲」「利用者要件」「受け入れ条件」という分類
- 初心者向けの短い用語説明

### 10.3 後続の設計書で決めること

- DB設計書で、MySQLの各列の型、必須設定、既定値、自動設定などの具体的な定義を、本書のデータ要件に合わせて確定する。
- 画面設計書で、各画面の表示項目、入力部品、ボタン、移動、使用classを、本書の要件と `handout_app_spec.md` のCSS指定に合わせて具体化する。
